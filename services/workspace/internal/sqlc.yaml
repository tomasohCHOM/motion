version: "2"
sql:
  - engine: "postgresql"
    schema: "sql/schemas/"
    queries: "sql/queries/"
    gen:
      go:
        package: "models"
        sql_package: "pgx/v5"
        out: "models"
        json_tags_id_uppercase: true
        emit_json_tags: true
