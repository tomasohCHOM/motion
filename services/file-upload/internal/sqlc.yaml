version: "2"
sql:
  - engine: "postgresql"
    # schema: "sql/schemas/"
    queries: "sql/queries/"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "db"
        json_tags_id_uppercase: true
        emit_json_tags: true
